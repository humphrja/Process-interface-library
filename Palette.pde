class Palette {
  color primary, highlight, select, stroke;

  Palette(color prim, color hlht, color slct, color strk) {  
    primary = prim;
    highlight = hlht;
    select = slct;
    stroke = strk;
  }
}
